# Configuración de Base de Datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=la_repoteria_db
DB_PORT=3306

# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Configuración JWT (JSON Web Tokens)
JWT_SECRET=tu_clave_secreta_super_segura_cambiar_en_produccion_2024
JWT_EXPIRES_IN=24h

# IMPORTANTE: 
# 1. Copia este archivo como .env (sin el .example)
# 2. Cambia DB_PASSWORD con tu contraseña de MySQL
# 3. Cambia JWT_SECRET por una clave única y segura
# 4. NO subas el archivo .env a Git (ya está en .gitignore)
